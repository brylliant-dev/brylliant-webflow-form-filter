{
  "name": "brylliant-webflow-form-filter",
  "version": "1.0.0",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}